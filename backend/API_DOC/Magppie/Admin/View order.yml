info:
  name: View order
  type: http
  seq: 3

http:
  method: GET
  url: "{{HOST}}/v1/admin/orders/:orderId"
  params:
    - name: orderId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
