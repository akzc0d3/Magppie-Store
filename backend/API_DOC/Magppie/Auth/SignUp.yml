info:
  name: SignUp
  type: http
  seq: 2

http:
  method: POST
  url: "{{HOST}}/v1/auth/signup"
  body:
    type: form-urlencoded
    data:
      - name: email
        value: ""
      - name: password
        value: ""
      - name: role
        value: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
