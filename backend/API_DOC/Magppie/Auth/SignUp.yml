info:
  name: SignUp
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/v1/auth/signup
  body:
    type: form-urlencoded
    data:
      - name: email
        value: bom@deo.com
      - name: password
        value: 12Thuusudsd@ssdd
      - name: role
        value: user
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
