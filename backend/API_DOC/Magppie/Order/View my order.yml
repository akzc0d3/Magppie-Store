info:
  name: View my order
  type: http
  seq: 4

http:
  method: GET
  url: "{{HOST}}/v1/order/:orderId"
  params:
    - name: orderId
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
