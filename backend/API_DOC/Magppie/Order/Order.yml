info:
  name: Order
  type: http
  seq: 1

http:
  method: POST
  url: "{{HOST}}/v1/order/"
  body:
    type: json
    data: |-
      [
        {
          "id":"",
          "quantity":1
        },
        {
          "id":"",
          "quantity":10
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
