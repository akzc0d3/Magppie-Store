info:
  name: View my orders
  type: http
  seq: 2

http:
  method: GET
  url: "{{HOST}}/v1/order/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
