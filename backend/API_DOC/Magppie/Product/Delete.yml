info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{HOST}}/v1/product/remove"
  body:
    type: json
    data: |-
      [
        "productID-1",
        "productID-2"
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
